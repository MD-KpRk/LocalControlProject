<Window x:Class="Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server"
        mc:Ignorable="d"
        Title="Local Control. Подчинение" Height="450" Width="400" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:MyViewModel x:Key="viewmodel"></local:MyViewModel>
    </Window.Resources>

    <Grid DataContext="{StaticResource viewmodel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" Margin="5">
            <TextBlock Text="{Binding Text}" FontSize="15"/>
        </StackPanel>

        <TextBlock FontSize="72" Grid.Column="1" Text="{Binding Value}"></TextBlock>

    </Grid>
</Window>
